GOOGLE CALENDAR API
=================================

To run this application on server you should:
1. Dowload it form github git pull origin master
2. Run play framework activator
3. Run application start -Dhttp.address=0.0.0.0 -Dhttp.port=8080
You should use your server IP address.

