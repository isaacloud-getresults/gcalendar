# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

GET 	/a						controllers.Application.a()

POST 	/b 						controllers.Application.b()

GET		/deleteNotification			controllers.Application.deleteEvent()
	
GET		/meetingNotification		controllers.Application.meetingCheck()

GET		/meetingBoard				controllers.Application.meetingBoard()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
